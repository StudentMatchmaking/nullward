---
layout: base.njk
title: Tags
permalink: /tags/
---

<style>
.page-header {
    padding: 10rem 2rem 2rem;
    max-width: 900px;
    margin: 0 auto;
}

.page-title {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: 2.5rem;
    font-weight: 400;
    margin-bottom: 1rem;
}

.page-description {
    color: var(--text-secondary);
    font-size: 1.1rem;
}

.tags-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    max-width: 900px;
    margin: 0 auto;
    padding: 0 2rem 4rem;
}

.tag-card {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.25rem;
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    color: var(--text-secondary);
    text-decoration: none;
    font-size: 0.9rem;
    transition: all 0.2s ease;
}

.tag-card:hover {
    color: var(--text-primary);
    border-color: var(--text-muted);
}

.tag-count {
    font-size: 0.75rem;
    color: var(--text-muted);
}
</style>

<header class="page-header">
    <h1 class="page-title">Tags</h1>
    <p class="page-description">Browse essays by topic.</p>
</header>

<div class="tags-grid">
    {% for tag in collections.tagList %}
    <a href="/tags/{{ tag | slugify }}/" class="tag-card">
        {{ tag }}
        <span class="tag-count">({{ collections[tag].length }})</span>
    </a>
    {% endfor %}
</div>
